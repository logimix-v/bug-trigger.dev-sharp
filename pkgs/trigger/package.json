{
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc --watch",
    "prepare": "npm run build",
    "preview": "tsc -b",
    "deploy": "trigger.dev deploy",
    "clean": "rm -rf node_modules ; rm -rf dist ; rm -rf .trigger"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.675.0",
    "@aws-sdk/lib-storage": "^3.675.0",
    "@trigger.dev/sdk": "^3.0.13",
    "sharp": "^0.33.5",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@trigger.dev/build": "^3.0.13",
    "@types/node": "^22.7.7",
    "@types/uuid": "^10.0.0",
    "trigger.dev": "^3.0.13"
  }
}
